<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<configuration status="error" monitorInterval="30" packages="org.apache.logging.log4j.core,io.sentry.log4j2">

    <Properties>
        <Property name="dir">logs</Property>
<!--        <Property name="logFormat">[%d{yyyy-MM-dd HH:mm:ss:SSS}] [%-5level] [%t] [fin-cust-account] [%c(%L)] %m%n</Property>-->
        <Property name="logFormat">[%d{yyyy-MM-dd HH:mm:ss.SSS}][%thread][%traceId] %-5level %logger{56}.%method:%L -%msg%n</Property>
        <Property name="every_file_size">500M</Property>
        <Property name="log_level">info</Property>
    </Properties>

    <!--先定义所有的appender-->
    <appenders>
        <Sentry name="Sentry"></Sentry>
        <!--这个输出控制台的配置-->
        <console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout pattern="${logFormat}"/>
        </console>
        <!--文件会打印出所有信息，这个log每次运行程序会自动清空，由append属性决定，这个也挺有用的，适合临时测试用-->
        <File name="log" fileName="${dir}/test.log" append="false">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} %-5level %class{36} %L %M - %msg%xEx%n"/>
        </File>
        <!-- 这个会打印出所有的info及以下级别的信息，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <RollingFile name="RollingFileInfo" fileName="${dir}/info.log"
                     filePattern="${dir}/$${date:yyyy-MM}/info-%d{yyyy-MM-dd}-%i.log.zip">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${logFormat}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="${every_file_size}"/>
            </Policies>
        </RollingFile>
        <RollingFile name="RollingFileWarn" fileName="${dir}/warn.log"
                     filePattern="${dir}/$${date:yyyy-MM}/warn-%d{yyyy-MM-dd}-%i.log.zip">
            <ThresholdFilter level="warn" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${logFormat}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="${every_file_size}"/>
            </Policies>
            <!-- DefaultRolloverStrategy属性如不设置，则默认为最多同一文件夹下7个文件，这里设置了20 -->
            <DefaultRolloverStrategy max="20" compressionLevel="1"/>
        </RollingFile>
        <RollingFile name="RollingFileError" fileName="${dir}/error.log"
                     filePattern="${dir}/$${date:yyyy-MM}/error-%d{yyyy-MM-dd}-%i.log.zip">
            <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${logFormat}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="${every_file_size}"/>
            </Policies>
        </RollingFile>
        <GRPCLogClientAppender name="grpc-log">
            <PatternLayout pattern="%msg"/>
        </GRPCLogClientAppender>
    </appenders>
    <!--然后定义logger，只有定义了logger并引入的appender，appender才会生效-->
    <loggers>
        <logger name="digestLog" level="INFO" additivity="false">
            <!--            <appender-ref ref="Console"/>-->
            <appender-ref ref="grpc-log"/>
            <appender-ref ref="RollingFileInfo"/>
        </logger>
        <!--过滤掉spring和mybatis的一些无用的DEBUG信息-->
        <logger name="org.springframework" level="INFO"></logger>
        <logger name="org.mybatis" level="INFO"></logger>
        <root level="info">
            <appender-ref ref="Console"/>
            <appender-ref ref="RollingFileInfo"/>
            <appender-ref ref="RollingFileWarn"/>
            <appender-ref ref="RollingFileError"/>
            <appender-ref ref="grpc-log"/>
            <Appender-ref ref="Sentry"/>
        </root>
    </loggers>
</configuration>